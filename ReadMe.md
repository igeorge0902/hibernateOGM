# hibernate OGM

A sample project to demonstrate Java Persistence API working witn n datasource. It means a single back-end point can serve as a SPOC (single point of contact) to access as many databases as configured from literally within the application, which therefore serves as the brain.